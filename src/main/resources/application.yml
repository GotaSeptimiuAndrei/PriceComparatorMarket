spring:
  application:
    name: PriceComparatorMarket

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/pricecomparatormarket?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

  mail:
    host: smtp.gmail.com
    port: 587
    username: "gotaseptimiuandrei@gmail.com"
    password: "qnhotlsglvgjhfok"
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: 8080

